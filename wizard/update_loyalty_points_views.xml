<?xml version="1.0" encoding="utf-8"?>
<odoo>

    <!-- Vista Formulario del Wizard -->
    <record id="view_update_loyalty_points_form" model="ir.ui.view">
        <field name="name">partner.loyalty.update.points.form</field>
        <field name="model">partner.loyalty.update.points</field>
        <field name="arch" type="xml">
            <form string="Actualizar Puntos de Fidelidad">
                <group>
                    <field name="points_to_add"/>
                </group>
                <footer>
                    <button name="action_apply" type="object"
                        string="Aplicar" class="btn-primary"
                        data-hotkey="q"/>
                    <button string="Cancelar" class="btn-secondary"
                        special="cancel" data-hotkey="x"/>
                </footer>
            </form>
        </field>
    </record>

    <!-- Acción del Wizard: aparece en el menú Acción de la lista de contactos -->
    <record id="action_update_loyalty_points" model="ir.actions.act_window">
        <field name="name">Actualizar Puntos</field>
        <field name="res_model">partner.loyalty.update.points</field>
        <field name="view_mode">form</field>
        <field name="target">new</field>
        <field name="binding_model_id" ref="base.model_res_partner"/>
        <field name="binding_view_types">list</field>
    </record>

</odoo>
